'use strict';

var assert = require('assert');
var hasChildSelector = require('..');

describe('has-child-selector', function() {

  it('should do something awesome', function() {
    assert.equal(hasChildSelector(), true);
  });
});
